# 09 - Route Groups

We use `( )` to create route groups.

This it as a group rather than a route

For example:

```bash
├───(auth)
│   ├───login
|   |   └───page.tsx
│   ├───register
|   |   └───page.tsx
│   ├───forgotPassword
|   |   └───page.tsx
```

